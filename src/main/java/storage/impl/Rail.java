package storage.impl;

import storage.GeoObject;
import storage.Node;
import storage.Point;

public class Rail extends GeoObject {
    public Rail(int id,ArrayList<Node> nodes) {
        super(id,nodes);
    }
}
