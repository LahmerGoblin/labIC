package storage.impl;

import storage.GeoObject;
import storage.Node;
import storage.Point;

public class Tunnel extends GeoObject {
    public Tunnel(int id,ArrayList<Node> nodes) {
        super(id,nodes);
    }
}
